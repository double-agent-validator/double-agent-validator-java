
import { expect } from 'chai';

describe('DoubleAgentValidatorNg2Factory', () => {
  it('loads the script and builds the DoubleAgentValidator service', () => {
    expect(1 + 1).to.be.equal(2);
  });
});
